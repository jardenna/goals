export interface InputListProps {
  inputIdentifier: string;
  label: string;
  name: string;
  value: string;
  checked?: boolean;
  error?: string;
  hidden?: boolean;
  isRequired?: boolean;
  placeholder?: string;
  type?: string;
}
